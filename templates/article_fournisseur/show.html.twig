{% extends 'base.html.twig' %}

{% block title %}ArticleFournisseur{% endblock %}

{% block body %}
    <h1>ArticleFournisseur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_fournisseur.id }}</td>
            </tr>
            <tr>
                <th>Prix_actuel</th>
                <td>{{ article_fournisseur.prixActuel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_article_fournisseur_index') }}">back to list</a>

    <a href="{{ path('app_article_fournisseur_edit', {'id': article_fournisseur.id}) }}">edit</a>

    {{ include('article_fournisseur/_delete_form.html.twig') }}
{% endblock %}
