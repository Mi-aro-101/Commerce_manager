{% extends 'base.html.twig' %}

{% block title %}BonCommande index{% endblock %}

{% block body %}
    <h1>BonCommande index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>DateLivraison</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for bon_commande in bon_commandes %}
            <tr>
                <td>{{ bon_commande.id }}</td>
                <td>{{ bon_commande.dateLivraison ? bon_commande.dateLivraison|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_bon_commande_show', {'id': bon_commande.id}) }}">show</a>
                    <a href="{{ path('app_bon_commande_edit', {'id': bon_commande.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_bon_commande_new') }}">Create new</a>
{% endblock %}
