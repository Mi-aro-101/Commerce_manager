{% include 'header_finance.html.twig' %}


{% block title %} Bon Commande{% endblock %}


<div class="content-wrapper" style="min-height: 1302.4px;">

<section class="content">
<div class="container-fluid">
<div class="row">
<div class="col-md-6">
<div class="card card-primary">
     <div class="card-header">
            <h3 class="card-title"> Envoie bon de commande </h3>
    </div>
    <div class="card-body">
{% block body %}
     {{ form_start(form) }}
    {# <div class="row">
        <div class="col-sm-6">
            <div class="form-group">
                <label> Fournisseur </label>
                <select name="{{ field_name(form.article) }}" class="form-control">
                    {% for label, value in field_choices(form.article) %}
                        <option value="{{ value }}">{{ label }}</option>
                    {% endfor %}
                </select>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="form-group">
            <label> Quantite </label>
            <input type="text" class="form-control" name="{{ field_name(form.quantite) }}">
        </div>
    </div> #}

    <div class="row">
        <div class="col-sm-6">
            <div class="form-group">
                <label> Date Envoie bon de commande </label>
                {{ form_row(form.dateEnvoi, 
                    {'attr': {'class':'form-control', 'id': 'exampleInputPassword1'}
                }) }}
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-sm-6">
            <div class="form-group">
                <label> Date de livraison </label>
                {{ form_row(form.dateLivraison, 
                    {'attr': {'class':'form-control', 'id': 'exampleInputPassword1'}
                }) }}
            </div>
        </div>
    </div>

    <div class="row">
        <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
    </div>
  {{ form_end(form) }}
    <a href="{{ path('app_demande_departement_index') }}">back to list</a>
{% endblock %}

</div>
</div>
</div>
</div>
</section>
</div>

{% include 'footer_template.html.twig' %}