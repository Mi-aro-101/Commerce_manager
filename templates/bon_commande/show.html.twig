{% extends 'base.html.twig' %}

{% block title %}BonCommande{% endblock %}

{% block body %}
    <h1>BonCommande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bon_commande.id }}</td>
            </tr>
            <tr>
                <th>DateLivraison</th>
                <td>{{ bon_commande.dateLivraison ? bon_commande.dateLivraison|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bon_commande_index') }}">back to list</a>

    <a href="{{ path('app_bon_commande_edit', {'id': bon_commande.id}) }}">edit</a>

    {{ include('bon_commande/_delete_form.html.twig') }}
{% endblock %}
