<form method="post" action="{{ path('app_c_v_candidat_delete', {'id': c_v_candidat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ c_v_candidat.id) }}">
    <button class="btn">Delete</button>
</form>
