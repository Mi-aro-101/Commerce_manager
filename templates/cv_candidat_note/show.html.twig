{% extends 'base.html.twig' %}

{% block title %}CvCandidatNote{% endblock %}

{% block body %}
    <h1>CvCandidatNote</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cv_candidat_note.id }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ cv_candidat_note.Note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cv_candidat_note_index') }}">back to list</a>

    <a href="{{ path('app_cv_candidat_note_edit', {'id': cv_candidat_note.id}) }}">edit</a>

    {{ include('cv_candidat_note/_delete_form.html.twig') }}
{% endblock %}
