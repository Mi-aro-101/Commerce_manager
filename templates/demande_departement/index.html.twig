{% extends 'base.html.twig' %}

{% block title %}DemandeDepartement index{% endblock %}

{% block body %}
    <h1>DemandeDepartement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantite</th>
                <th>Date</th>
                <th>Statut</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for demande_departement in demande_departements %}
            <tr>
                <td>{{ demande_departement.id }}</td>
                <td>{{ demande_departement.quantite }}</td>
                <td>{{ demande_departement.date ? demande_departement.date|date('Y-m-d') : '' }}</td>
                <td>{{ demande_departement.statut }}</td>
                <td>
                    <a href="{{ path('app_demande_departement_show', {'id': demande_departement.id}) }}">show</a>
                    <a href="{{ path('app_demande_departement_edit', {'id': demande_departement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_demande_departement_new') }}">Create new</a>
{% endblock %}
