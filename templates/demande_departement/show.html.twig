{% extends 'base.html.twig' %}

{% block title %}DemandeDepartement{% endblock %}

{% block body %}
    <h1>DemandeDepartement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ demande_departement.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ demande_departement.quantite }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ demande_departement.date ? demande_departement.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ demande_departement.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_demande_departement_index') }}">back to list</a>

    <a href="{{ path('app_demande_departement_edit', {'id': demande_departement.id}) }}">edit</a>

    {{ include('demande_departement/_delete_form.html.twig') }}
{% endblock %}
