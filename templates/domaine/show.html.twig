{% extends 'base.html.twig' %}

{% block title %}Domaine{% endblock %}

{% block body %}
    <h1>Domaine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ domaine.id }}</td>
            </tr>
            <tr>
                <th>Id_domaine</th>
                <td>{{ domaine.idDomaine }}</td>
            </tr>
            <tr>
                <th>Des_domaine</th>
                <td>{{ domaine.desDomaine }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_domaine_index') }}">back to list</a>

    <a href="{{ path('app_domaine_edit', {'id': domaine.id}) }}">edit</a>

    {{ include('domaine/_delete_form.html.twig') }}
{% endblock %}
