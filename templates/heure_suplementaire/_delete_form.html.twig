<form method="post" action="{{ path('app_heure_suplementaire_delete', {'id': heure_suplementaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ heure_suplementaire.id) }}">
    <button class="btn">Delete</button>
</form>
