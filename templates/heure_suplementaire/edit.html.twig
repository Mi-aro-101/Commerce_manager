{% extends 'base.html.twig' %}

{% block title %}Edit HeureSuplementaire{% endblock %}

{% block body %}
    <h1>Edit HeureSuplementaire</h1>

    {{ include('heure_suplementaire/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_heure_suplementaire_index') }}">back to list</a>

    {{ include('heure_suplementaire/_delete_form.html.twig') }}
{% endblock %}
