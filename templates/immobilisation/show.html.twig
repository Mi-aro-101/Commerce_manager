{% extends 'base.html.twig' %}

{% block title %}Immobilisation{% endblock %}

{% block body %}
    <h1>Immobilisation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ immobilisation.id }}</td>
            </tr>
            <tr>
                <th>Etat_avance</th>
                <td>{{ immobilisation.etatAvance }}</td>
            </tr>
            <tr>
                <th>Taux_amortissement</th>
                <td>{{ immobilisation.tauxAmortissement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_immobilisation_index') }}">back to list</a>

    <a href="{{ path('app_immobilisation_edit', {'id': immobilisation.id}) }}">edit</a>

    {{ include('immobilisation/_delete_form.html.twig') }}
{% endblock %}
