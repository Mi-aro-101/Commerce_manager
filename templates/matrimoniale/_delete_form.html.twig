<form method="post" action="{{ path('app_matrimoniale_delete', {'id': matrimoniale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ matrimoniale.id) }}">
    <button class="btn">Delete</button>
</form>
