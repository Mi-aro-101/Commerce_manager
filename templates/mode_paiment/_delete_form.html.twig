<form method="post" action="{{ path('app_mode_paiment_delete', {'id': mode_paiment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mode_paiment.id) }}">
    <button class="btn">Delete</button>
</form>
