<form method="post" action="{{ path('app_section_reponse_delete', {'id': section_reponse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ section_reponse.id) }}">
    <button class="btn">Delete</button>
</form>
