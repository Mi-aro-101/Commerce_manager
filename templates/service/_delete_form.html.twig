<form method="post" action="{{ path('app_service_delete', {'id_service': service.getId()}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.getId()) }}">
    <button class="btn">Delete</button>
</form>
