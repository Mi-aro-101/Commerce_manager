{% extends 'base.html.twig' %}

{% block title %}TestAptitude{% endblock %}

{% block body %}
    <h1>TestAptitude</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ test_aptitude.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_test_aptitude_index') }}">back to list</a>

    <a href="{{ path('app_test_aptitude_edit', {'id': test_aptitude.id}) }}">edit</a>

    {{ include('test_aptitude/_delete_form.html.twig') }}
{% endblock %}
